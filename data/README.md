# Data

This directory contains the datasets used throughout the project.

## Purpose

The `data` directory separates raw, processed, and sample datasets to keep the data pipeline organized and reproducible.

## Structure

```text
data/
├── raw/
├── processed/
└── sample/